// 12S23035 - Julius Kaisar Bona Theo Sinaga 

#include <stdio.h>
#define kali 1
#define tambah 0
#define kurang 0

int main(int _argc, char **_argv) {

    // Declare variables
    char operan;
    int one, two, three, result;

    // Input
    scanf("%c", &operan);
    if (operan = '+') {
        one = 0;
    } else if (operan = '-') {
        one = 0;
    } else if (operan = '*') {
        one = 0;
    } else {
        
    }


    return 0;

}
